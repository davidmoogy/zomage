[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["dotnet restore", "dotnet publish -c Release -o ./publish"]

[start]
cmd = "cd ./publish && ASPNETCORE_URLS=http://0.0.0.0:$PORT dotnet zomage.dll"

