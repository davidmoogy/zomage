# Railway Deployment Guide - ZOMAGE

## 🚀 Railway-ზე Deployment-ის ნაბიჯები

### 1. GitHub Repository-ს შექმნა

თუ ჯერ არ გაქვთ GitHub repository:

1. გადადით [github.com](https://github.com)
2. შექმენით ახალი repository
3. დააკომიტეთ თქვენი კოდი:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/zomage.git
git push -u origin main
```

### 2. Railway-ზე რეგისტრაცია

1. გადადით [railway.app](https://railway.app)
2. დააჭირეთ **"Start a New Project"** ან **"Login"**
3. აირჩიეთ **"Login with GitHub"**
4. დაუშვით Railway-ს წვდომა თქვენს GitHub repositories-ზე

### 3. Project-ის შექმნა

1. Railway Dashboard-ში დააჭირეთ **"New Project"**
2. აირჩიეთ **"Deploy from GitHub repo"**
3. აირჩიეთ თქვენი `zomage` repository
4. Railway ავტომატურად დაიწყებს deployment-ს

### 4. Environment Variables (სურვილისამებრ)

Railway ავტომატურად აყენებს `PORT` variable-ს. თუ გსურთ დამატებითი:

1. Project-ში გადადით **"Variables"** tab-ზე
2. დაამატეთ:
   - `ASPNETCORE_ENVIRONMENT` = `Production`
   - `ASPNETCORE_URLS` = `http://0.0.0.0:$PORT`

### 5. Deployment-ის შემოწმება

1. Railway ავტომატურად გააკეთებს build და deploy-ს
2. Build logs-ში უნდა ჩანდეს:
   ```
   dotnet publish -c Release -o ./publish
   cd ./publish && dotnet zomage.dll
   ```
3. Deployment-ის შემდეგ მიიღებთ URL-ს, მაგალითად:
   ```
   https://zomage-production.up.railway.app
   ```

### 6. Custom Domain (სურვილისამებრ)

1. Project-ში გადადით **"Settings"** tab-ზე
2. **"Domains"** სექციაში დაამატეთ თქვენი domain
3. Railway მოგცემთ DNS records-ს რომლებიც უნდა დაამატოთ

## 📝 მნიშვნელოვანი შენიშვნები

- **Database**: SQLite ფაილი იქმნება ავტომატურად Railway-ზე
- **Static Files**: `wwwroot` folder ავტომატურად serve-დება
- **Port**: Railway ავტომატურად აყენებს PORT environment variable-ს
- **Build**: Railway იყენებს `dotnet publish` build command-ს

## 🔧 Troubleshooting

### Build Fails
- შეამოწმეთ რომ `zomage.csproj`-ში არის `net8.0`
- შეამოწმეთ რომ `Microsoft.EntityFrameworkCore.Sqlite` version არის `8.0.0`

### App Doesn't Start
- შეამოწმეთ logs Railway Dashboard-ში
- დარწმუნდით რომ `PORT` variable არის set

### Database Issues
- SQLite ფაილი იქმნება ავტომატურად `EnsureCreated()`-ით
- Production mode-ში ბაზა არ წაიშლება

## 💰 Pricing

Railway-ს აქვს **უფასო tier**:
- $5 credit თვეში
- საკმარისია პატარა აპლიკაციებისთვის
- თუ გადააჭარბებთ, გადახდა მოგიწევთ

## 🎉 Success!

თუ ყველაფერი სწორად გააკეთეთ, თქვენი აპლიკაცია უნდა იყოს live Railway-ზე!

